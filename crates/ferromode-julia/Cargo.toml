[package]
name = "ferromode-julia"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Julia bindings for Ferromode via C-ABI shared library"

[lib]
name = "ferromode"
crate-type = ["cdylib"]

[dependencies]
ferromode = { path = "../ferromode" }

[lints]
workspace = true
