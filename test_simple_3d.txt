use ferromode::adapters::multidim::{Volume3D, decompose_volume_3d_separable};
use ferromode::algorithms::emd::EmdConfig;

// Test with uniform data (should always work)
let data = vec![100.0; 4*4*4];
let vol = Volume3D::new(4, 4, 4, data, None)?;

// Try decomposition
let config = EmdConfig::default();
let result = decompose_volume_3d_separable(&vol, &config, false)?;
